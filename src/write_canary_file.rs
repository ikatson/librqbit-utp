use std::io::{BufReader, BufWriter, Write};

#[test]
fn write_canary_file() {
    // let f = std::fs::OpenOptions::new()
    //     .create(true)
    //     .write(true)
    //     .truncate(true)
    //     .open("/tmp/canary")
    //     .unwrap();
    // let mut f = BufWriter::new(f);

    // for i in 0usize..2 * 1024 * 1024 * 1024 {
    //     f.write_all(&[i as u8]).unwrap();
    // }
}
